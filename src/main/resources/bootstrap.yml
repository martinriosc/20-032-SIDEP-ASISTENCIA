spring:
  application:
    name: sidep-sostenedor-api
  cloud:
    config:
      failFast: true
      uri: http://dockerdesa.mineduc.cl/properties-centralizados/
      server:
        native:
          searchLocations: classpath:/
